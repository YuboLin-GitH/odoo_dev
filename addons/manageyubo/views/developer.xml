<odoo>
  <data>

    <record model="ir.actions.act_window" id="action_developer_window">
        <field name="name">Developers</field>
        <field name="res_model">res.partner</field> <field name="view_mode">tree,form</field>
        
        <field name="domain">[('is_dev', '=', True)]</field>

        <field name="context">{'default_is_dev': True}</field>
    </record>

    <record model="ir.ui.view" id="manage.devs_partner_form">
        <field name="name">manage devs form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="mode">primary</field> 
        <field name="arch" type="xml">
            <xpath expr="//sheet/notebook/page[@name='internal_notes']" position="after">
                <page name="devs" string="Devs">
                    <group>
                        <field name="is_dev"/> <field name="technologies"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

    <record model="ir.actions.act_window.view" id="action_view_developer_tree">
        <field name="sequence" eval="1"/>
        <field name="view_mode">tree</field>
        <field name="view_id" ref="base.view_partner_tree"/> <field name="act_window_id" ref="manage.action_developer_window"/>
    </record>

    <record model="ir.actions.act_window.view" id="manage.action_view_developer_form">
        <field name="sequence" eval="2"/>
        <field name="view_mode">form</field>
        <field name="view_id" ref="manage.devs_partner_form"/> <field name="act_window_id" ref="manage.action_developer_window"/>
    </record>

    <menuitem name="Devs" id="manage.menu_1_devs_list" 
              parent="menu_manageyubo_management"
              action="manage.action_developer_window"/>

  </data>
</odoo>